"""Postbuild passes applied after the v3.0.0 predecessor transform.

FY27 help text and the workbook palette are the tracked successors of the one-off
session scripts that produced v3.1.0. Later passes, including GST help text,
start from the committed `ozzit.xlsx` and `src/` recorded in ATTRIBUTION.md.
They do not read the predecessor workbook.
"""
